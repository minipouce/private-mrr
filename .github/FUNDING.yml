# Adds a "Sponsor" button to the repository. `custom` accepts up to four URLs.
# Deliberately untracked: GitHub renders the full URL in the sidebar, and a
# query string there reads as clutter. The README link carries the UTM instead.
custom: ["https://buska.io"]
